import Game from './js/game.js';

window.addEventListener('DOMContentLoaded', () => {
	// Instantiate game - stored in const but not referenced again
	// eslint-disable-next-line no-unused-vars
	const game = new Game();
});
